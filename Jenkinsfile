pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Build started'
                sh 'pwd'
                sh 'ls -l'
            }
        }

        stage('Test') {
            steps {
                echo 'Test running'
                sh 'date'
            }
        }

    }
}
